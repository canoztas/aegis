# Aegis SAST - Local HuggingFace Models (GPU/CUDA)
# Install with: pip install -r requirements/requirements-gpu.txt
#
# Requirements:
#   - NVIDIA GPU with CUDA 11.8+ drivers
#   - ~8GB+ VRAM recommended for 7B models

# Core dependencies
flask>=3.0.0
werkzeug>=3.0.0
requests>=2.31.0
aiohttp>=3.9.0
pydantic>=2.0.0
cryptography>=41.0.0
python-magic>=0.4.27
pyyaml>=6.0
python-dotenv>=1.0.0

# Cloud AI Providers
openai>=1.0.0
anthropic>=0.18.0
google-genai>=1.0.0

# PyTorch with CUDA 11.8 support
--extra-index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0

# HuggingFace Transformers
transformers>=4.30.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# ONNX Runtime with GPU support
onnxruntime-gpu>=1.15.0

# GPU Acceleration & Quantization
accelerate>=0.20.0
bitsandbytes>=0.41.0
