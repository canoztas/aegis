# Minimal Requirements (Cloud LLMs Only - No Local Models)
# Install with: pip install -r requirements-minimal.txt
# Use this for deployments that only use OpenAI/Anthropic/Google APIs

# Core Web Framework
flask>=3.0.0
werkzeug>=3.0.0

# HTTP and Networking
requests>=2.31.0
aiohttp>=3.9.0

# Data Validation
pydantic>=2.0.0

# Cloud AI Providers
openai>=1.0.0
anthropic>=0.18.0
google-generativeai>=0.3.0

# Security & Cryptography
cryptography>=41.0.0

# File Handling
python-magic>=0.4.27

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# Note: This excludes torch, transformers, and onnxruntime
# which are ~3GB+ in size and only needed for local models
